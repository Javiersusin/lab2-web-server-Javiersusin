server:
  port: 8443
  ssl:
    enabled: true
    key-store: classpath:localhost.p12
    key-store-password: mesasilla1
    key-store-type: PKCS12
    key-alias: localhost
  http2:
    enabled: true

